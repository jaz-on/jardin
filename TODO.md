# Jardin — suite possible

Ce fichier liste des pistes hors périmètre de la release `0.1.0`. Le suivi détaillé peut vivre dans les issues GitHub.

- Filtrer les Query Loop archive `event` (futur vs passé) entièrement côté blocs ou via hooks plugin, si la méta seule ne suffit pas.
- Style variations FSE (optionnel, non prioritaire) : exposer les palettes notées dans `theme.json` (`theme-variants`) comme variations d’éditeur si besoin un jour.
- Reading time, sidenotes, thème clair/sombre (voir `docs/03-mvp-specifications.md` post‑MVP).